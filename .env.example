# Optional in local development; required for canonical URLs and sitemap in production.
PUBLIC_SITE_URL=https://example.com

# Required when the site is deployed below the domain root (for example, GitHub project pages).
PUBLIC_BASE_PATH=/repository-name/
